<% if params[:search] %>
  <% provide(:title, "Organization Search Results") %>
<% else %>
  <% provide(:title, "All Organizations") %>
<% end %>

<%= render 'search_form' %>


<ol>
  <%= render @organizations %>
</ol>
